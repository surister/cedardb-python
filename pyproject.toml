[project]
name = "cedardb"
version = "0.0.1"
description = "Python driver for CedarDB "
requires-python = ">=3.11"
dependencies = [
    "psycopg>=3.2.6",
]

[dependency-groups]
dev = [
    "black>=25.1.0",
    "pytest>=8.3.5",
]
